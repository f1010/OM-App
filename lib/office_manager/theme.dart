import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors extends MaterialColor {
  static const Color darkViolet = Color(0XFF673AB8);
  static const Color lightBlue = Color(0XFF4A90E2);
  static const Color lightViolet = Color(0XFFFEF9F6);

  AppColors(primary, swatch) : super(primary, swatch);
}
